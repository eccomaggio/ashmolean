Vibe-coded app to iteratively build up a sequence of python regex expressions which can be applied to other similar OCR texts.
Use the GUI to load files.
Transformed text is saved as transform.txt
Regex patterns are saved as patterns.txt